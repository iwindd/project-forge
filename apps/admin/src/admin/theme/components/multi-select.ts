import { MantineThemeComponents } from "@mantine/core";

const MultiSelect = {
  defaultProps: {
    comboboxProps: {
      shadow: "xl",
    },
  },
} satisfies MantineThemeComponents["MultiSelect"];

export default MultiSelect;
