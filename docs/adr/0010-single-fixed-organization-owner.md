# Single fixed organization owner

**Status: accepted.** Every organization has exactly one `OWNER`, created with the organization, and the current scope does not allow assigning another member as `OWNER`. The owner cannot be suspended, removed, or downgraded while the organization is active. `ADMIN` members handle delegated organization administration; owner transfer is deliberately outside the current domain so there is no ambiguous ownership or multiple-owner state.
