# Recreate the un-deployed schema and seed new data

**Status: accepted.** The application has no deployed data that needs to be preserved, so the approved domain/schema change will use a new schema and seed data rather than backfilling the current project, role, session, or identity records. This is not an authorization to reset the database now; any destructive reset or recreation is deferred until the schema and domain plan is approved and must be an explicit command.
