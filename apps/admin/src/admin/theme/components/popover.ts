import { MantineThemeComponents } from "@mantine/core";

const Popover = {
  defaultProps: {
    withArrow: true,
    arrowOffset: 12,
    shadow: "xl",
  },
} satisfies MantineThemeComponents["Popover"];

export default Popover;
