import { MantineThemeComponents } from "@mantine/core";

const Menu = {
  defaultProps: {
    withArrow: true,
    arrowOffset: 12,
    shadow: "xl",
  },
} satisfies MantineThemeComponents["Menu"];

export default Menu;
