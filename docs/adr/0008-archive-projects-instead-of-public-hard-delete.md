# Archive projects instead of exposing public hard delete

**Status: accepted.** Project removal from the normal admin workflow is an archive operation and remains reversible through the project lifecycle. The API does not expose public hard delete; irreversible deletion is reserved for explicit maintenance operations outside the project management contract.
