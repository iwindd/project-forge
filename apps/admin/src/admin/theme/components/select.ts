import { MantineThemeComponents } from "@mantine/core";

const Select = {
  defaultProps: {
    comboboxProps: {
      shadow: "xl",
    },
  },
} satisfies MantineThemeComponents["Select"];

export default Select;
