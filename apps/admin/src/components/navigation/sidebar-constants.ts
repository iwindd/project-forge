/**
 * Fixed sidebar width. The CSS variable intentionally bypasses Mantine's
 * numeric size conversion so the width stays 260px regardless of scale.
 */
export const SIDEBAR_WIDTH = 'var(--admin-sidebar-width)';
